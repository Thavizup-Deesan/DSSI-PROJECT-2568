# 📡 Request - Response Flow Guide

## 1️⃣ การรับ-ส่งข้อมูล (Data Flow Architecture)

### **Communication Layers**

```
┌─────────────────────────────────────────────────────────────────────┐
│                     FRONTEND (HTML/JavaScript)                      │
│  - ผู้ใช้ interact กับ UI (คลิก, พิมพ์, ส่ง form)                    │
│  - Browser จัดทำ Request ไปยัง Backend                              │
└───────────────────┬─────────────────────────────────────────────────┘
                    │
      ┌─────────────┴─────────────┐
      │  HTTP Protocol             │
      │  (Request/Response)        │
      │  over Network              │
      └─────────────┬─────────────┘
                    │
┌───────────────────▼─────────────────────────────────────────────────┐
│                    BACKEND (Django Server)                          │
│  - รับ HTTP Request                                                 │
│  - ประมวลผลข้อมูล (CRUD operations)                                 │
│  - ส่ง HTTP Response กลับไป                                         │
└───────────────────┬─────────────────────────────────────────────────┘
                    │
      ┌─────────────┴─────────────┐
      │  Database Driver           │
      │  (MySQL Protocol)          │
      └─────────────┬─────────────┘
                    │
┌───────────────────▼─────────────────────────────────────────────────┐
│                    DATABASE (MySQL)                                 │
│  - เก็บข้อมูลในตาราง (Tables)                                       │
│  - คืนข้อมูลให้ Django                                               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2️⃣ HTTP Request/Response Cycle (วงจรการรับส่ง)

```
      BROWSER (Client)                        DJANGO SERVER (Backend)
           │                                           │
           │  1. USER ACTION                          │
           │  (คลิกปุ่ม/กรอกฟอร์ม)                    │
           │◄──────────────────────────────────────┐  │
           │                                       │  │
           │  2. JAVASCRIPT CREATES REQUEST        │  │
           │─────────────────────────────────────► │  │
           │     (method, endpoint, data)          │  │
           │                                       │  │
           │  3. HTTP REQUEST SENT                 │  │
           ├──────────────────────────────────────►│  │
           │                                       │  │
           │                          ┌────────────┼──┤
           │                          │  DJANGO    │  │
           │                          │  PROCESSING│  │
           │                          │  - Route   │  │
           │                          │  - View    │  │
           │                          │  - Model   │  │
           │                          │  - Database│  │
           │                          └────────────┼──┤
           │                                       │  │
           │◄──────────────────────────────────────┤  │
           │  4. HTTP RESPONSE RECEIVED            │  │
           │  (status code + JSON data)            │  │
           │                                       │  │
           │  5. JAVASCRIPT PROCESSES               │  │
           │──────────────────────────────────────►│  │
           │  (update DOM, show data)              │  │
           │                                       │  │
      Browser UI Updated                     Server Ready
```

---

## 3️⃣ HTTP Headers และ Body Structure

### **REQUEST Structure (ส่งไป Backend)**

```
┌─────────────────────────────────────┐
│    HTTP REQUEST LINE                │
│ METHOD PATH HTTP/VERSION            │
│ POST /api/projects/ HTTP/1.1        │
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│    REQUEST HEADERS                  │
│ Host: localhost:8000                │
│ Content-Type: application/json      │
│ X-CSRFToken: abc123def456           │
│ Content-Length: 125                 │
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│    BLANK LINE                       │
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│    REQUEST BODY                     │
│ {                                   │
│   "project_code": "PRJ-001",        │
│   "project_name": "ระบบ A",        │
│   "budget_total": 150000,           │
│   "status": "ดำเนินการ"             │
│ }                                   │
└─────────────────────────────────────┘
```

### **RESPONSE Structure (ส่งกลับ Frontend)**

```
┌─────────────────────────────────────┐
│    HTTP STATUS LINE                 │
│ HTTP/1.1 201 Created                │
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│    RESPONSE HEADERS                 │
│ Content-Type: application/json      │
│ Content-Length: 145                 │
│ Date: Sat, 15 Nov 2025 10:30:00 GMT│
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│    BLANK LINE                       │
└─────────────────────────────────────┘
           │
┌─────────────────────────────────────┐
│    RESPONSE BODY                    │
│ {                                   │
│   "project_id": 1,                  │
│   "project_code": "PRJ-001",        │
│   "project_name": "ระบบ A",        │
│   "budget_total": "150000.00",      │
│   "status": "ดำเนินการ"             │
│ }                                   │
└─────────────────────────────────────┘
```

---

## 4️⃣ ตัวอย่าง Request/Response แบบปกติ (Normal CRUD)

### **4.1 READ - ดึงรายชื่อโครงการ (GET)**

#### **Frontend JavaScript Code:**

```javascript
// Step 1: Create Request
async function fetchProjects() {
  const response = await fetch("/api/projects/", {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
    },
  });

  // Step 2: Parse Response
  const data = await response.json();
  return data;
}

// ใช้ function
fetchProjects().then((projects) => {
  console.log("ดึงข้อมูลสำเร็จ:", projects);
});
```

#### **HTTP Raw Format:**

```
REQUEST (ส่งไป Backend):
─────────────────────────────────────
GET /api/projects/ HTTP/1.1
Host: localhost:8000
Content-Type: application/json
Connection: keep-alive
Accept: application/json

(ไม่มี body สำหรับ GET request)

─────────────────────────────────────

RESPONSE (ส่งกลับ Frontend):
─────────────────────────────────────
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 356

[
  {
    "project_id": 1,
    "project_code": "PRJ-001",
    "project_name": "ระบบสารสนเทศเขต",
    "budget_total": "150000.00",
    "status": "ดำเนินการ"
  },
  {
    "project_id": 2,
    "project_code": "PRJ-002",
    "project_name": "ปรับปรุงห้องสมุด",
    "budget_total": "75000.00",
    "status": "ปิด"
  }
]
```

---

### **4.2 CREATE - เพิ่มโครงการใหม่ (POST)**

#### **Frontend JavaScript Code:**

```javascript
async function createProject(projectData) {
  const response = await fetch("/api/projects/", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "X-CSRFToken": getCookie("csrftoken"), // ⚠️ Django Security Token
    },
    body: JSON.stringify(projectData), // ✅ ส่ง data เป็น JSON
  });

  if (!response.ok) {
    const error = await response.json();
    throw new Error(error);
  }
  return await response.json();
}

// ใช้ function
const newProject = {
  project_code: "PRJ-003",
  project_name: "โปรเจคใหม่",
  budget_total: 200000,
  status: "ดำเนินการ",
};

createProject(newProject)
  .then((result) => console.log("บันทึกสำเร็จ:", result))
  .catch((error) => console.error("Error:", error));
```

#### **HTTP Raw Format:**

```
REQUEST (ส่งไป Backend):
─────────────────────────────────────
POST /api/projects/ HTTP/1.1
Host: localhost:8000
Content-Type: application/json
X-CSRFToken: eJ0x1234abcd5678efgh
Content-Length: 120

{
  "project_code": "PRJ-003",
  "project_name": "โปรเจคใหม่",
  "budget_total": 200000,
  "status": "ดำเนินการ"
}

─────────────────────────────────────

RESPONSE (ส่งกลับ Frontend):
─────────────────────────────────────
HTTP/1.1 201 Created
Content-Type: application/json
Location: /api/projects/PRJ-003/
Content-Length: 125

{
  "project_id": 3,
  "project_code": "PRJ-003",
  "project_name": "โปรเจคใหม่",
  "budget_total": "200000.00",
  "status": "ดำเนินการ"
}
```

---

### **4.3 UPDATE - แก้ไขโครงการ (PUT)**

#### **Frontend JavaScript Code:**

```javascript
async function updateProject(projectCode, updatedData) {
  const response = await fetch(`/api/projects/${projectCode}/`, {
    method: "PUT",
    headers: {
      "Content-Type": "application/json",
      "X-CSRFToken": getCookie("csrftoken"),
    },
    body: JSON.stringify(updatedData),
  });

  return await response.json();
}

// ใช้ function
const updates = {
  project_name: "ระบบสารสนเทศเขต (ปรับปรุง)",
  budget_total: 160000,
};

updateProject("PRJ-001", updates).then((result) =>
  console.log("แก้ไขสำเร็จ:", result)
);
```

#### **HTTP Raw Format:**

```
REQUEST (ส่งไป Backend):
─────────────────────────────────────
PUT /api/projects/PRJ-001/ HTTP/1.1
Host: localhost:8000
Content-Type: application/json
X-CSRFToken: eJ0x1234abcd5678efgh
Content-Length: 92

{
  "project_name": "ระบบสารสนเทศเขต (ปรับปรุง)",
  "budget_total": 160000
}

─────────────────────────────────────

RESPONSE (ส่งกลับ Frontend):
─────────────────────────────────────
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 132

{
  "project_id": 1,
  "project_code": "PRJ-001",
  "project_name": "ระบบสารสนเทศเขต (ปรับปรุง)",
  "budget_total": "160000.00",
  "status": "ดำเนินการ"
}
```

---

### **4.4 DELETE - ลบโครงการ (DELETE)**

#### **Frontend JavaScript Code:**

```javascript
async function deleteProject(projectCode) {
  const response = await fetch(`/api/projects/${projectCode}/`, {
    method: "DELETE",
    headers: {
      "X-CSRFToken": getCookie("csrftoken"),
    },
  });

  if (response.status === 204) {
    console.log("ลบสำเร็จ");
    return true;
  }
  throw new Error("ลบไม่สำเร็จ");
}

// ใช้ function
deleteProject("PRJ-003")
  .then(() => console.log("ลบข้อมูลสำเร็จ"))
  .catch((error) => console.error("Error:", error));
```

#### **HTTP Raw Format:**

```
REQUEST (ส่งไป Backend):
─────────────────────────────────────
DELETE /api/projects/PRJ-003/ HTTP/1.1
Host: localhost:8000
X-CSRFToken: eJ0x1234abcd5678efgh
Content-Length: 0

(ไม่มี body)

─────────────────────────────────────

RESPONSE (ส่งกลับ Frontend):
─────────────────────────────────────
HTTP/1.1 204 No Content
Content-Length: 0

(ไม่มี body - ลบสำเร็จ)
```

---

## 5️⃣ ตัวอย่าง Request/Response - Import Excel

### **Frontend JavaScript Code:**

```javascript
async function importExcel(file) {
  const formData = new FormData();
  formData.append("importFile", file); // 📁 ส่งไฟล์

  const response = await fetch("/api/projects/import-excel/", {
    method: "POST",
    headers: {
      "X-CSRFToken": getCookie("csrftoken"),
    },
    body: formData, // 📝 ไม่ใช่ JSON - เป็น FormData
  });

  return await response.json();
}

// ใช้ function
const fileInput = document.getElementById("importFile");
fileInput.addEventListener("change", (e) => {
  importExcel(e.target.files[0]).then((result) => console.log(result.message));
});
```

### **HTTP Raw Format:**

```
REQUEST (ส่งไป Backend):
─────────────────────────────────────
POST /api/projects/import-excel/ HTTP/1.1
Host: localhost:8000
X-CSRFToken: eJ0x1234abcd5678efgh
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="importFile"; filename="projects.xlsx"
Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet

[Binary file content...]
------WebKitFormBoundary7MA4YWxkTrZu0gW--

─────────────────────────────────────

RESPONSE (ส่งกลับ Frontend):
─────────────────────────────────────
HTTP/1.1 201 Created
Content-Type: application/json
Content-Length: 68

{
  "message": "Successfully imported/updated 10 projects."
}

--- ERROR RESPONSE (ถ้าไฟล์ผิด) ---
HTTP/1.1 400 Bad Request
Content-Type: application/json
Content-Length: 85

{
  "error": "Missing required columns in Excel file"
}
```

---

## 6️⃣ HTTP Status Codes ที่ใช้ในระบบ

| Status Code | Meaning            | ตัวอย่างกรณีใช้                 |
| ----------- | ------------------ | ------------------------------- |
| **200**     | OK                 | GET สำเร็จ, PUT สำเร็จ          |
| **201**     | Created            | POST สำเร็จ, file upload สำเร็จ |
| **204**     | No Content         | DELETE สำเร็จ                   |
| **400**     | Bad Request        | ข้อมูลไม่ถูกต้อง, ไฟล์ผิด       |
| **404**     | Not Found          | ไม่พบโครงการ                    |
| **405**     | Method Not Allowed | ใช้ HTTP method ผิด             |
| **500**     | Server Error       | Error ที่ Backend               |

---

## 7️⃣ Request/Response Flow - ทั้งหมด (Complete Cycle)

### **Scenario: ผู้ใช้ดึงรายชื่อโครงการแล้วเพิ่มโครงการใหม่**

```
┌─────────────────────────────────────────────────────────────────────┐
│                      STEP 1: PAGE LOAD                              │
└─────────────────────────────────────────────────────────────────────┘

  Browser ← | JavaScript: switchTab('projects')

  ┌───────────────────────────────────────┐
  │   REQUEST 1                           │
  │ GET /api/projects/ HTTP/1.1           │
  │ Host: localhost:8000                  │
  └───────────────────────────────────────┘
           │
           ▼ (ส่งไป Backend)

  Backend ← | Django: ProjectViewSet.list()
           | SQL: SELECT * FROM projects
           | MySQL: [Query DB]

  ┌───────────────────────────────────────┐
  │   RESPONSE 1                          │
  │ HTTP/1.1 200 OK                       │
  │ [                                     │
  │   {...project 1...},                  │
  │   {...project 2...}                   │
  │ ]                                     │
  └───────────────────────────────────────┘
           │
           ▼ (ส่งกลับ Browser)

  Browser → | JavaScript: render table
            | DOM: <tr><td>PRJ-001</td>...
            | Display: ✅ Table updated


┌─────────────────────────────────────────────────────────────────────┐
│                   STEP 2: USER CLICKS "ADD"                         │
└─────────────────────────────────────────────────────────────────────┘

  Browser ← | User: Click "เพิ่มโครงการ"
            | JavaScript: Open modal form


┌─────────────────────────────────────────────────────────────────────┐
│                STEP 3: USER FILLS FORM & SUBMIT                     │
└─────────────────────────────────────────────────────────────────────┘

  Browser ← | User: Type project data & click "บันทึก"
            | JavaScript: dataForm.addEventListener('submit')

  ┌───────────────────────────────────────┐
  │   REQUEST 2                           │
  │ POST /api/projects/ HTTP/1.1          │
  │ Content-Type: application/json        │
  │ X-CSRFToken: eJ0x1234abcd5678efgh    │
  │                                       │
  │ {                                     │
  │   "project_code": "PRJ-004",          │
  │   "project_name": "โปรเจคใหม่",      │
  │   "budget_total": 300000,             │
  │   "status": "ดำเนินการ"               │
  │ }                                     │
  └───────────────────────────────────────┘
           │
           ▼ (ส่งไป Backend)

  Backend ← | Django: ProjectViewSet.create()
           | Serializer: Validate data
           | ORM: Projects.objects.create(...)
           | SQL: INSERT INTO projects (...)
           | MySQL: INSERT success ✅


  ┌───────────────────────────────────────┐
  │   RESPONSE 2                          │
  │ HTTP/1.1 201 Created                  │
  │                                       │
  │ {                                     │
  │   "project_id": 4,                    │
  │   "project_code": "PRJ-004",          │
  │   "project_name": "โปรเจคใหม่",      │
  │   "budget_total": "300000.00",        │
  │   "status": "ดำเนินการ"               │
  │ }                                     │
  └───────────────────────────────────────┘
           │
           ▼ (ส่งกลับ Browser)

  Browser → | JavaScript: alert("บันทึกข้อมูลสำเร็จ!")
            | JavaScript: switchTab('projects') [reload]
            | DOM: Table updated with new row
            | Display: ✅ New project appears in table
```

---

## 8️⃣ Browser DevTools - ดูการรับส่งข้อมูล

### **วิธีดูใน Chrome/Firefox:**

1. **เปิด DevTools**: `F12` หรือ `Ctrl+Shift+I`
2. **ไปที่แท็บ Network**
3. **ทำการ action ใดๆ** (เช่น click "เพิ่มโครงการ")
4. **ดูรายการ request**:
   - **Name**: endpoint (เช่น `/api/projects/`)
   - **Method**: HTTP method (GET, POST, PUT, DELETE)
   - **Status**: Response status (200, 201, 400, etc.)
   - **Type**: Request type (xhr = fetch)

### **ดูรายละเอียด Request/Response:**

- **Headers tab**: ดูการตั้งค่า request headers
- **Payload tab**: ดูข้อมูลที่ส่งไป (request body)
- **Response tab**: ดูข้อมูลที่ได้รับ (response body)

---

## 9️⃣ Error Handling - เมื่อเกิดข้อผิดพลาด

### **Error Flow Example:**

```
Frontend Request → Backend Processing Error

REQUEST:
─────────────────────────────────────────────
POST /api/projects/ HTTP/1.1
{
  "project_code": "",  ⚠️ Empty!
  "project_name": "Test"
}

─────────────────────────────────────────────

RESPONSE:
─────────────────────────────────────────────
HTTP/1.1 400 Bad Request
Content-Type: application/json

{
  "project_code": ["This field may not be blank."]
}

─────────────────────────────────────────────

JavaScript Error Handler:
─────────────────────────────────────────────
if (!response.ok) {
  const error = await response.json();
  console.error('Validation Error:', error);
  alert('⚠️ ' + error.project_code[0]);
}
```

---

## 🔟 สรุป: Request/Response Pattern ในระบบ

| Operation | HTTP Method | Endpoint                      | Request Body   | Response Status |
| --------- | ----------- | ----------------------------- | -------------- | --------------- |
| Read All  | GET         | `/api/projects/`              | ❌ ไม่มี       | 200 + [array]   |
| Read One  | GET         | `/api/projects/{code}/`       | ❌ ไม่มี       | 200 + object    |
| Create    | POST        | `/api/projects/`              | ✅ JSON object | 201 + object    |
| Update    | PUT         | `/api/projects/{code}/`       | ✅ JSON object | 200 + object    |
| Delete    | DELETE      | `/api/projects/{code}/`       | ❌ ไม่มี       | 204 (no body)   |
| Import    | POST        | `/api/projects/import-excel/` | ✅ FormData    | 201 + message   |

---

## ✅ ท็อพ 5 สิ่งที่ต้องจำ

1. **Method ที่ถูกต้อง**: GET (อ่าน), POST (สร้าง), PUT (แก้ไข), DELETE (ลบ)
2. **Content-Type Header**: `application/json` สำหรับ JSON data
3. **X-CSRFToken**: ⚠️ **บังคับ** สำหรับ POST, PUT, DELETE (Django Security)
4. **Status Code**: ตรวจสอบว่า Response status ถูกต้อง
5. **Error Handling**: Always check `response.ok` ก่อนใช้ data
