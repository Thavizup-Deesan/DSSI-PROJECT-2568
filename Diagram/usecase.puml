@startuml
left to right direction
skinparam packageStyle rectangle

' === Actors ===
actor "ผู้ขอซื้อ (Requester)" as User
actor "หัวหน้าภาควิชา (Head of Dept)" as Boss
actor "คณะกรรมการตรวจรับ (Committee)" as Committee

' Note for Offline Roles
note right of Boss : เซ็นเอกสาร Offline เท่านั้น\n(ไม่ได้เข้าใช้งานระบบ)
note right of Committee : สแกน QR Code เพื่อดูรายการ\n(ไม่ต้อง Login)

package "POTMS System" {
    ' === Authentication ===
    usecase "UC-01 เข้าสู่ระบบ (Login)" as UC01
    usecase "UC-02 ลงทะเบียน (Register)" as UC02
    usecase "UC-03 ออกจากระบบ (Logout)" as UC03
    
    ' === User Actions (Requester) ===
    usecase "UC-04 เลือกโครงการ (Select Project)" as UC04
    usecase "UC-05 สร้างใบขอซื้อ (Create Order)" as UC05
    usecase "UC-06 ดูรายการใบขอซื้อ (View My Orders)" as UC06
    usecase "UC-07 แก้ไขใบขอซื้อ (Edit Order)" as UC07
    usecase "UC-08 ส่งขออนุมัติ (Submit Order)" as UC08
    usecase "UC-09 บันทึกการรับของ (Record Receiving)" as UC09
    usecase "UC-10 ดูประวัติ/ค้นหา/Export (Order History)" as UC10
    usecase "UC-11 อัปโหลดรูปภาพประกอบ (Upload Order Images)" as UC11
    
    ' === Committee Actions ===
    usecase "UC-12 สแกน QR ดูรายการ (Scan QR View Items)" as UC12
    
    ' === System Processes ===
    usecase "สร้าง QR Code (Generate QR)" as UC_GenQR
}

' === Relationships ===

' Authentication (Requester Only)
User -- UC01
User -- UC02
User -- UC03

' User Flow (Requester)
User -- UC04
User -- UC05
User -- UC06
User -- UC07
User -- UC08
User -- UC09
User -- UC10
User -- UC11

' Committee Flow (No Login, just Scan)
Committee -- UC12

' Offline interactions (representation only, no system link)
' Boss has no direct link to system use cases as per requirement

' Include Relationships
UC08 ..> UC_GenQR : <<include>>\n(สร้าง QR อัตโนมัติเมื่อส่งออเดอร์)

@enduml