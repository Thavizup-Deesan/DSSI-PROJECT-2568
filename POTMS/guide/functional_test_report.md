# รายงานผลการทดสอบระบบ (Functional Test Report) - POTMS

ตารางแสดงผลการทดสอบฟังก์ชันการทำงานหลัก (Functional Testing)
**ระบบที่ทดสอบ:** ระบบบริหารจัดการพัสดุ (POTMS)
**วันที่ทดสอบ:** 14 กุมภาพันธ์ 2569

## 1. ระบบยืนยันตัวตนและการจัดการสิทธิ์ (Authentication & Role Management)

| รหัสการทดสอบ | กรณีทดสอบ (Test Case) (ข้อมูลทดสอบ) | ผลลัพธ์ที่คาดหวัง (Expected Result) | ผลการทดสอบ (Actual Result) | สรุปผล |
| :--- | :--- | :--- | :--- | :--- |
| **TC-AUTH-01** | **Login ด้วย Google Account**<br>ผู้ทดสอบ: `somchai.r@ubu.ac.th` | เข้าสู่ระบบได้ และระบบสร้าง User Profile: "Somchai R." | เข้าสู่ระบบสำเร็จ (Redirect ไปยังหน้า Dashboard) และพบชื่อ "Somchai R." มุมขวาบน | **ผ่าน** |
| **TC-AUTH-02** | **กำหนดสิทธิ์ผู้ใช้งาน**<br>ผู้ทดสอบ: `admin@ubu.ac.th`<br>เปลี่ยนสิทธิ์ `somchai.r` เป็น `Head` | ผู้ใช้ `somchai.r` ต้องเห็นเมนู "อนุมัติ PR" | เปลี่ยนสิทธิ์สำเร็จ และเมื่อ Login ด้วย `somchai.r` พบเมนู "Approve Orders" ปรากฏขึ้น | **ผ่าน** |

## 2. ระบบจัดการโครงการ (Project Management)

| รหัสการทดสอบ | กรณีทดสอบ (Test Case) (ข้อมูลทดสอบ) | ผลลัพธ์ที่คาดหวัง (Expected Result) | ผลการทดสอบ (Actual Result) | สรุปผล |
| :--- | :--- | :--- | :--- | :--- |
| **TC-PROJ-01** | **สร้างโครงการใหม่**<br>โครงการ: "โครงการอบรม AI เพื่อการศึกษา"<br>รหัส: `P-68-001`<br>งบวัสดุ: 50,000 บาท | ระบบบันทึกข้อมูลโครงการ สถานะเป็น `Active` และโชว์ยอดงบวัสดุ 50,000 | บันทึกสำเร็จ ข้อมูลโครงการ `P-68-001` ปรากฏในรายการ สถานะ `Active` งบประมาณถูกต้อง | **ผ่าน** |
| **TC-PROJ-02** | **แก้ไขงบประมาณ**<br>แก้ไขงบใช้สอยจาก 0 เป็น 10,000 บาท | ยอดงบรวมของโครงการต้องเพิ่มขึ้นตามที่แก้ไข | แก้ไขสำเร็จ ยอดงบรวมเปลี่ยนเป็น 60,000 บาท (เดิม 50,000 + 10,000) | **ผ่าน** |

## 3. ระบบจัดซื้อจัดจ้าง (Procurement Process)

| รหัสการทดสอบ | กรณีทดสอบ (Test Case) (ข้อมูลทดสอบ) | ผลลัพธ์ที่คาดหวัง (Expected Result) | ผลการทดสอบ (Actual Result) | สรุปผล |
| :--- | :--- | :--- | :--- | :--- |
| **TC-PROC-01** | **สร้างใบขอซื้อ (Create PR)**<br>ผู้ขอซื้อ: `somying.j@ubu.ac.th`<br>รายการ: "กระดาษ A4 Double A"<br>จำนวน: 10 รีม<br>ราคาต่อหน่วย: 150 บาท | สร้าง PR สำเร็จ เลขเอกสาร `PO-68-0001`<br>ราคารวม = 1,500 บาท<br>สถานะ: `Draft` | สร้างสำเร็จ ได้เลข `PO-68-0001` ยอดรวม 1,500 บาท สถานะเป็น `Draft` ถูกต้อง | **ผ่าน** |
| **TC-PROC-02** | **อนุมัติใบขอซื้อ (Approve PR)**<br>ผู้อนุมัติ: `somchai.r` (Head)<br>เอกสาร: `PO-68-0001` | สถานะเปลี่ยนเป็น `Pending_Receive` | กดปุ่ม Approve สำเร็จ สถานะเปลี่ยนเป็น `Pending_Receive` พร้อมให้รับของต่อ | **ผ่าน** |

## 4. ระบบตรวจรับพัสดุ (Receiving & Inspection)

| รหัสการทดสอบ | กรณีทดสอบ (Test Case) (ข้อมูลทดสอบ) | ผลลัพธ์ที่คาดหวัง (Expected Result) | ผลการทดสอบ (Actual Result) | สรุปผล |
| :--- | :--- | :--- | :--- | :--- |
| **TC-RECV-01** | **รับของบางส่วน (Partial Receive)**<br>รับรายการ: "กระดาษ A4"<br>จำนวนที่รับ: 5 รีม (จาก 10)<br>ไฟล์แนบ: `receipt_001.jpg` | บันทึกการรับได้ สถานะเปลี่ยนเป็น `Pending_Inspection` ยอดคงเหลือ = 5 | บันทึกสำเร็จระบบแสดงยอดรับ 5/10 สถานะเป็น `Pending_Inspection` | **ผ่าน** |
| **TC-RECV-02** | **ดูรูปภาพใบเสร็จ**<br>คลิกไอคอนรูปภาพที่รายการรับของ | แสดงรูปภาพ `receipt_001.jpg` ในหน้าต่างใหม่ | รูปภาพแสดงผลชัดเจน (ไม่พบ Error 404) | **ผ่าน** |
| **TC-INSP-01** | **ตรวจรับพัสดุ (Inspection)**<br>กรรมการ: `committee1@ubu.ac.th`<br>ผลการตรวจ: "ผ่าน" (Pass) | สถานะเปลี่ยนเป็น `Inspected` ตัดยอดออกจาก PR | บันทึกผลตรวจสำเร็จ สถานะเป็น `Inspected` ยอดรับสมบูรณ์ | **ผ่าน** |

## 5. ระบบการเบิกจ่ายและปิดโครงการ (Payment & Closure)

| รหัสการทดสอบ | กรณีทดสอบ (Test Case) (ข้อมูลทดสอบ) | ผลลัพธ์ที่คาดหวัง (Expected Result) | ผลการทดสอบ (Actual Result) | สรุปผล |
| :--- | :--- | :--- | :--- | :--- |
| **TC-PAY-01** | **เบิกจ่ายเงิน (Payment)**<br>จ่ายเงินให้ร้านค้า สำหรับ `PO-68-0001`<br>ยอดจ่าย: 1,500 บาท | สถานะใบสั่งซื้อเปลี่ยนเป็น `Completed` ตัดงบโครงการ | บันทึกสถานะการจ่ายเงินสำเร็จ สถานะ PO เป็น `Completed` งบโครงการลดลงถูกต้อง | **ผ่าน** |

**หมายเหตุ:** ข้อมูลข้างต้นเป็นข้อมูลทดสอบ (Mock Data) ใช้เพื่อการตรวจสอบระบบเท่านั้น
